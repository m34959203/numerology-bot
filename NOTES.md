# NOTES — живой журнал работы

> Чек-поинты по ходу задачи. Продолжать отсюда.

## 2026-06-10 (часть 2) — Полные описания из Excel (видео-ТЗ заказчика)

Заказчик (видео): система должна выдавать не цифры, а **полные текстовые описания** к
каждому параметру. Заказчица уточнила: описания **уже в ячейках Excel** (кликабельны),
и **меняются вместе со значением** (= VLOOKUP). → извлечь+вывести, не выдумывать.

**Глубоко прочёсан лист «текст» cell-by-cell** (1635 непустых ячеек, строки 2–401; карта
в `docs/content-extraction-map.md`). Каждая строка учтена. Найдено и сделано:
- **Персональное число года** — выводится полным текстом (~4000 симв, `personal_year.json`),
  а не короткой меткой. `loader.interpret_prefixed` (ключи «N - заголовок») + `moon_sun`.
- **ЧЖП + Число души** — структурные поля УЖЕ были извлечены `extract_texts` как списки
  аспектов, но не выводились с подписями. Добавлен лейблинг (`labeled_aspects` + `_ASPECT_LABELS`
  в render): ЧЖП → Предназначение/Недостатки/Профессии/Цвет удачи/Талисман; Число души →
  10 полей (эмоции/отношения/числа/камни/цвета/болезни/рекомендации). Текст+PDF.
- **2 новых блока извлечены** (`extract_texts` REGISTRY + регенерация; существующие JSON не
  тронуты): `life_force_graph` (0–9, «график кода жизни»), `moon_year`/`sun_year`/`year_total`
  (Луна/Солнце/ИТОГ по году; `sun_year[0]` = текст нулевого периода).
- **Прогноз подключён per-год**: `report.build_forecast_section` обогащает каждый год
  лукапом по ЕГО значениям (moon/sun/year_value/rebirth_cycle) → render/pdf показывают «Итог
  года» (что ждёт/можно/нельзя), «⚠️ Нулевой период» при Солнце=0, «Жизненные силы» в
  судьбоносный год. `matrix.py` НЕ тронут (остаётся golden). 87 тестов зелёные, ruff/black чисто.

**СВЕРКА МЕТОДИК с формулами Excel (data_only=False):**
- ✅ ИТОГ года: листы 29-33 `D6=VLOOKUP(Matr!H29..33, текст!H363:I401)`, `Matr!H=Солнце−Луна`
  = наш `year_value`; эталон Ерофеева [-2,3,11,3,-1] совпал 1:1. Лукап верен.
- ⚠️ «График кода жизни» (лист 19): ключ `INDEX(F21:K21, MAX(F39:F51))` — ОДИН показатель,
  НЕ `rebirth_cycle`/не per-год. Моё подключение было неверным → **удалил** (правило: не
  выдумывать). JSON оставлен, ключ реверсить позже.
- ✅ Код «до/после 35» (лист 18): два кода — `1-код=РАСЧЕТ!AR30=VALUE(calc!E18&E19)`(=human_code),
  `2-код=РАСЧЕТ!AV30=VALUE(calc!E20&E21)`; оба через кодировки. «35» — подпись, не switch.

**2-КОД «ПОСЛЕ 35» — СДЕЛАНО (2026-06-10).** Реверс `calc!E18..E21` сошёлся: 1-код =
`{spiritual}{be7}` = human_code (235); 2-код = `{bf7}{bg7}` (1910) — обе величины уже
считались в `codes.py`, добавил наружу `Codes.second_code`. `report` тянет описания обоих
из `human_code.json`; render/pdf выводят блок «Кодировка жизни»: «До 35 лет — код 235 …» /
«После 35 лет — код 1910 …». Эталон Ерофеева точно (235/1910). 88 тестов, ruff/black чисто.

**ГРАФИК КОДА ЖИЗНИ — РЕВЕРС КЛЮЧА ЗАВЕРШЁН (2026-06-10).** Лист 19: `F37=INDEX(F21:K21,
MAX(F39:F51))`. Разобрано: F21:K21 = 6 цифр AR32=AN11=код жизни (796000); F22:K34 = сетка
возрастов 0–77 по 6 в ряд; MAX = (age%6)+1. Итог: **активная цифра = код_жизни[age%6]**.
Эталон: 796000, возраст 36 → 7 (=F37). `matrix.life_code_graph_digit`, подключено per-год
(каждый год прогноза: «График кода жизни (код N): …»). 90 тестов, ruff/black чисто.

**ЕЖЕДНЕВНЫЙ ПРОГНОЗ НА ВЫБРАННУЮ ДАТУ — СДЕЛАНО (2026-06-10).** Формат «на выбранную
дату» (не дамп 365 дней). `core/numerology/daily.py` `daily_forecast(person, target_date)`:
ЧПД + трактовка (`personal_day.json`) + ЧПГ/ЧПМ контекст + статус биоритма дня
(`biorhythm.phase_on`: благоприятный/критический/травмоопасный/обычный). reference_date =
выбранная дата (compute_personal_numbers golden-протестирован). Рендер `render.render_daily`.
Бот-флоу `bot/handlers/daily.py` + `DailyStates` (дата рождения → дата прогноза, кнопка
«Сегодня»): кнопка в главном меню «📅 Прогноз на день» + команда `/day`; бесплатно (нужна
только дата рождения). `validators.parse_target_date` (допускает будущее, 1900–2100). Роутер
зарегистрирован в main. 95 тестов зелёные, ruff/black чисто.

ЦЕНА «ДЕТСКИХ МАТРИЦ» = 5000₸ — ПОДТВЕРЖДЕНО заказчиком 10.06.2026 (не заглушка).
Прайс полный: 1г 3000 / 5л 4500 / мини 5000 / полный 10000 / финансовый 8000 / детские
5000 / совместимость 15000.

ОТКРЫТО: всё из видео-ТЗ заказчика закрыто, цены подтверждены. Остаётся: merge PR #2
(`feat/tariff-fields-spec`) и решение по деплою (после merge — пересид каталога + рестарт).

## 2026-06-10 — Тарифы по спецификации заказчика (СДЕЛАНО, ветка feat/tariff-fields-spec)

Заказчик прислал точный состав каждого тарифа (WhatsApp, скрин в переписке). Закрыт
главный долг «тарифы не дифференцированы». Перешли с грубого выбора **по секциям** на
выбор **по полям** (атомам).

- `core/numerology/tariffs.py` переписан: атомарные FIELDS (18 шт.), `_SECTION_OF`,
  `TariffSpec(fields, forecast_years, manual, needs_parents, needs_name, contact_url)`,
  `.sections` выводится из полей. `report_for` прикрепляет к отчёту `_fields` (список атомов).
- Рендереры (`core/render.py`, `core/pdf.py`) гейтят под-блоки по `_want(report, atom)`;
  без `_fields` (legacy build_report) показывают всё. «Число души» — только при возрасте <30.
  Скаляры психоматрицы (ЧЖП/код поведения/уровень сознания/число души) теперь идут с
  **полным описанием** (interpret), а не только числом.
- Новое в расчётах: `matrix.current_energy_trend` → блок «энергопотенциал (на спаде/подъёме)»;
  `moon_sun.compute_personal_numbers` заполняет `personal_year_text` из `PERSONAL_YEAR_TEXT`.
- Каталог (`bot/catalog_data.py`): +`finance_1y` (8000₸), +`children` (5000₸). Цены: 1г 3000,
  5л 4500, мини 5000, полный 10000, финансовый 8000, детские 5000, совместимость 15000.
- **Ручные тарифы** `children` + `compatibility` (manual=True): бот НЕ считает, карточка ведёт
  кнопкой на мастера `https://t.me/+bZiSEb_asXZkN2M1` (методик детского/парного разбора в Excel нет).
  `catalog.cb_service_card`/`cb_pay` маршрутизируют по `spec_for(code).manual`.
- `finance_1y` собирает даты родителей (заказчик: «обязательно имя родителей и год рождения»).
  `survey._flags` теперь из `spec.needs_parents/needs_name`, не из секций.
- Тесты переписаны под полевую модель + новый `test_catalog_routing.py`. **81 тест зелёный,
  ruff/black чисто.** Смоук: все 7 тарифов рендерят текст+PDF без падений.

ОТКРЫТО (не блокирует): цены детских (5000 — заглушка, ждём подтверждения), «Код жизни»
не имеет текста-описания в Excel (показываем число). Деплой: после merge — пересид каталога
(`seed_services` синхронит по code) + рестарт.

## 2026-06-06 — Деплой (Docker + Alembic) СДЕЛАНО

- `Dockerfile` (multi-stage: builder ставит пакет с data-JSON в /install → slim-рантайм
  + fonts-dejavu-core, non-root appuser, entrypoint). `docker/entrypoint.sh`:
  `alembic upgrade head` → `python -m bot.main`. `.dockerignore`, `docker-compose.yml`
  (postgres16 + bot, healthcheck, env из .env). `docs/deploy.md` (compose / одиночный образ /
  миграции / бэкап / чек-лист).
- Alembic init-миграция `migrations/versions/e47bb79859ce_init_schema.py` (все 6 таблиц).
  env.py: asyncpg→psycopg для sync-Alembic; добавлен `psycopg[binary]` в deps. mako-шаблон
  обновлён под современный typing. package-data в pyproject (JSON-трактовки в wheel).
- **Образ собран и проверен в контейнере:** миграции применяются, PDF генерится (73КБ),
  импорты+данные+шрифт DejaVu на месте, 6 роутеров. Образ ~353МБ. 32 теста зелёные.

**Проект готов к боевому запуску.** От заказчика нужно: BOT_TOKEN, сервер с Docker,
заполнить .env (BOT_TOKEN/ADMIN_IDS/POSTGRES_PASSWORD), `docker compose up -d --build`.
Открыто по расчётам (не блокирует): число/карма имени (ФИО-эталоны), 2-е кармособытие (BG20-баг).

## 2026-06-06 — Этап 6: PDF-выдача (СДЕЛАНО)

`core/pdf.py` (reportlab): PDF-отчёт с кириллицей (DejaVuSans, поиск по системным путям
или `PDF_FONT_PATH`). `bot/delivery.py` — выдача по `REPORT_FORMAT` (text/pdf/both, дефолт
pdf) с graceful fallback на текст при сбое PDF. Подключено в анкету и «Мои расчёты».
reportlab → основные deps; CI ставит `fonts-dejavu-core`. Проверено pypdf: 6 страниц,
кириллица рендерится, значения верны, 0 литеральных тегов (экранируется только динамика,
`<b>/<br/>` остаются разметкой). 32 теста зелёные.

**Весь функционал из ТЗ заказчика готов:** меню, оплата Stars, анкета, авторасчёт по
формулам книги, выдача (текст + PDF). Для боевого запуска нужны: BOT_TOKEN, PostgreSQL,
Alembic-миграция, деплой (Docker), приёмка. Открыто по расчётам: число/карма имени
(ФИО-эталоны) и 2-е кармическое событие (BG20-баг).

## 2026-06-06 — Этап 1: инфраструктура бота (СДЕЛАНО)

Расчётный слой закрыт ранее → собрал бота вокруг ядра. Всё чисто импортируется,
диспетчер из 6 роутеров, 31 тест зелёный, lint чист.
- `core/db.py` (async engine/session), `core/repositories.py` (CRUD + **идемпотентный
  платёж** по charge_id), `core/render.py` (отчёт → plain-text, разбивка 4096),
  `core/validators.py` (даты ДД.ММ.ГГГГ, имена кириллица+kk).
- `bot/`: start (меню), catalog (услуги/карточка/invoice XTR), payment (pre_checkout +
  successful_payment → запись + запуск анкеты), survey (FSM ФИО+дата → confirm → расчёт+
  выдача split), results («Мои расчёты», повторная выдача), admin (статистика, /refund).
- `bot/main.py`: on_startup = create_all + seed_services; polling. Каталог-сид в
  `bot/catalog_data.py` (цена placeholder 100⭐). Порядок «оплата → анкета».
- Тесты: `test_render`, `test_validators`, `test_repositories` (aiosqlite). aiosqlite в dev-deps.

**Для прод-запуска осталось:** реальный `BOT_TOKEN` + PostgreSQL (Alembic-миграция вместо
create_all), приёмочный прогон живого бота. **Расчёты от ИМЕНИ** (число/карма имени) и
2-е кармическое событие (BG20-баг) — по-прежнему открыты (ФИО-эталоны). PDF-выдача — Этап 6.

**ВАЖНО (среда):** в окружении нет `libreoffice-calc` (только -core) → автопересчёт xlsx
здесь невозможен; сверка с Excel — через кэш заполненной книги (как делалось, 3 примера 1:1).

## 2026-06-05 — каркас + психоматрица

**Сделано:**
- Каркас репо по стандарту dev-base (README/CLAUDE/CI/шаблоны/Alembic/ORM) — CI зелёный.
- Excel-файл получен (`docs/ПРОГРАММА МАТРИЦА на русском.xlsx`), разобрана структура 50 листов.
- **Психоматрица (лист calc) реализована и верифицирована** против кэша Excel
  (golden 01.01.2000) → `core/numerology/psychomatrix.py`, `tests/test_psychomatrix.py`.
- Таблица «код поведения» (13 кодов по датам) → `core/content/data/behavior_codes.json`.
- Разбор алгоритмов задокументирован → `docs/excel-analysis.md`.

**Инсайт:** книга хранит кэш-значения для примера 01.01.2000 → можно строить
golden-тесты до получения контрольных примеров заказчика (`data_only=True`).

**Биоритмы (Bio) — СДЕЛАНО (2026-06-05, второй заход):**
- `core/numerology/biorhythm.py` + golden-тест `biorhythm_2000_01_01.json`.
- Рекуррента (инкременты 1.0131.../1.0202.../1.0252...) + классификация дней
  (благоприятные R+S+T=3, критические =6, травмоопасные 11.5<I<12) дают даты книги 1:1.
- Открыт только формат вывода (число/окно дат на категорию) → OPEN_QUESTIONS.

**Тексты-трактовки — СДЕЛАНО:** экстрактор `scripts/extract_texts.py` (24 темы → JSON),
`loader.interpret()` (=VLOOKUP), блоки отчёта в `report.py` (психоматрица+дни), тесты зелёные.

## 2026-06-05 — ПОЛНЫЙ анализ таблицы (рамка: таблица = эталон, переносим 1:1)

Многоагентно разобраны ВСЕ оставшиеся листы (сверка с кэшем 01.01.2000, нулевой допуск),
результат сведён в `docs/excel-analysis.md`:
- **Matr** (ядро, 4715 формул): код человека/жизни, фин-код, доступ к деньгам, жизненные
  силы, опасный возраст, жизненная задача, годовая таблица BI59:158 (Луна/Солнце/тренд по
  годам), кривая энергопотенциала BE59:158, прогноз 5 лет, кармические события. Всё от даты
  рождения; ФИО-зависимое (число/карма имени) = транслитерация, на пустом ФИО → 0.
- **Луна/Солнце по месяцам + ЧПМ** (листы Calculation/Lists луна и солнце, ЧПМ → render 35/36):
  движок помесячной Луны/Солнца и ЧПГ/ЧПМ/ЧПД. Имя НЕ используется. Сверено.
- **Сборка РАСЧЕТ**: все inline-тексты (C19/C21/AN14-17), кармособытия, прогноз 5 лет
  (полные switch P/AF/BO), порядок разделов, обязательные поля — выписаны.

**Баги книги к учёту** (чинить канонически): C21 (мёртвая ветка), AN17 (OR всегда TRUE),
BG20 (#VALUE дробный MID). См. excel-analysis R.1/R.2.

**Единственный реальный блокер для ФИО-расчётов:** нет эталона с заполненным ФИО →
«число имени»/«карма имени»/финпрогноз не верифицировать (см. OPEN_QUESTIONS).

## 2026-06-06 — получен ВТОРОЙ эталон с заполненным ФИО

Заказчик прислал заполненную книгу: **Миронов Дмитрий Владимирович, 28.05.1994**,
мать 06.08.1971, отец 23.11.1970, сегодня 06.06.2026.
- **Регрессия:** психоматрица (17 качеств + ЧЖП/число души/код поведения/уровень
  сознания/код жизни) совпала с новым эталоном ИДЕАЛЬНО → движок обобщается.
- **Фикс биоритмов:** display-слоты BD6/BD9/BD12 = 7/7/5. По двум эталонам уточнено:
  благоприятные = первые 7, травмоопасные = первые 5, **критические = окно 365 дней**
  (≤7), а не счётчик-5 (ex1→5, ex2→2). `DEFAULT_SPEC` в biorhythm.py. Оба примера в фикстурах.
- **РАЗБЛОКИРОВАНО:** теперь есть ФИО → можно реверсить число имени / карма имени /
  финпрогноз (Matr транслитерация). Кэш-эталоны для Этапа 3 (28.05.1994): полных лет 32,
  дух.уровень 38, код человека «3811», код жизни 279160, фин-код «1552», карма имени «нет».

## 2026-06-06 — ТРЕТИЙ эталон (женский) + Этап 3 коды

Заказчик прислал женский пример: **Ерофеева Юлия Владимировна, 20.02.1990, дев. Миронова**.
- Регрессия: психоматрица и биоритмы совпали ИДЕАЛЬНО (3-й независимый эталон; критич.=6
  снова в окне 365). Все три примера в фикстурах, тесты параметризованы (16 passed).
- **Реализован `codes.py`** (блок «Вычисления» от даты), сверен на ВСЕХ 3 эталонах:
  духовный уровень, код человека, счастливые числа, код жизни, фин-код, доступ к деньгам,
  жизненные силы, жизненная задача, полных лет, прожито дней. Общая цифровая логика
  вынесена в `_digits.py` (используют и psychomatrix, и codes).
- Урок: код человека/духовный уровень — от ДАТЫ, не от имени (BD7=Σцифр даты). От имени
  в РАСЧЕТ реально только «карма имени» (и та через баг OR → всегда «нет»).

## 2026-06-06 — Прогноз на 5 лет (matrix.py)

Реализован `compute_forecast` (Matr 29–33 → РАСЧЕТ), сверен с эталоном 20.02.1990:
год, возраст, **персональное число года** = свод(день+месяц+Σцифр года) + текст,
**Луна** = Σ первых 2 цифр (код_жизни/возраст), **Солнце** = Σ следующих 2,
**«год на +/−»** = Солнце−Луна + текст (−9..+9). Подключено в report.py. 17 passed.

## 2026-06-06 — судьбоносные годы + энергопотенциал + опасный возраст (РЕШЕНО)

Все три раскрыты и сверены на двух примерах (нулевой допуск), в `matrix.py`:
- **Судьбоносные годы (Z +/−):** цепочка `aₙ₊₁ = aₙ + Σцифр(aₙ)` от года рождения;
  в множество идут якоря (концы блоков по 4 цифры) и годы-повторы (прибавлялся 0).
  `fate_years(birth_year)`; Z="+" если год прогноза в множестве. (ex1: -,+,-,+,-; жен: +,-,-,-,-)
- **Энергопотенциал (J):** опоры = первые 5 цифр кода жизни (`MID(BO9,1..5)`), на
  возрастах 0,7,14,21,28,… циклически; линейная интерполяция /7. J=знак(e[A]−e[A−2]),
  при равенстве 1 если e[A−1]>4 иначе 0. Кривая age0..41 совпала с кэшем; J все 2 (жен).
- **Опасный возраст:** первый возраст ≥ текущего с Солнцем года=0 (ex1=25, жен.=нет).
  `danger_age()`, добавлен в report «Вычисления».

## 2026-06-06 — 12-летний цикл (РЕШЕНО) → ПРОГНОЗ 5 ЛЕТ ПОЛНОСТЬЮ ЗАКРЫТ

`AH(возраст) = digits5[(возраст//12) % 5]` (те же первые 5 цифр кода жизни, что и
энергокривая, но шаг 12 лет, не 7). Matr!BG[age]=CH[59+age//12], CH=digits5 цикл.
Флаг добро/зло = ISODD(AH); в отчёте AF — текст-свитч 0..9, показ при fate="+".
Сверено структурно по колонке BG (ages 0-47: 7,9,6,0 — включая ненулевые). 20 passed.

Прогноз 5 лет: year, age, personal_year(+текст), moon, sun, year_value(+текст),
fate(+/−), energy_potential, rebirth_cycle(+текст). + danger_age в «Вычислениях».

## 2026-06-06 — Этап 5: Луна/Солнце по месяцам + ЧПМ (РЕШЕНО)

`moon_sun.py`: помесячная Луна/Солнце (12 кодов J + тексты) и ЧПГ/ЧПМ/ЧПД (+тексты
месяца/дня/комбо). Сверено на женском эталоне 1:1 (J=[0,-7,12,-1,3,-3,0,5,0,0,9,5];
ЧПГ5/ЧПМ2/ЧПД8/комбо «2 − 5»). Тексты выгружены extract_texts.extract_moon_sun.
Гочи: «первые 4 цифры» дробного частного; ключ комбо U+2212. В report.py. 23 passed.

**Расчётный слой по таблице фактически закрыт.** Остаток (мелочь):
- число/карма имени — нужны ФИО-эталоны (в книге карма имени через баг OR всегда «нет»);
- кармические события BG17 (ок) / BG20 (баг #VALUE в книге — пересчитать чисто).

**Продолжить отсюда:**
1. Этап 1 — инфраструктура бота: меню, каталог, оплата Telegram Stars, FSM-анкета, БД.
2. Этап 6 — рендер отчёта (текст с разбивкой 4096 / PDF), админка.
2. Прогноз 5 лет (годовая таблица + энергокривая + Луна/Солнце) → `matrix.py`/`report.py`.
3. Луна/Солнце по месяцам + ЧПМ → `moon_sun.py` (Этап 5).
4. Число/карма имени — ПОСЛЕ получения ФИО-эталона от заказчика.

**Гочи биоритмов:** Excel ROUND = half-away-from-zero (не banker's). Аккумулятор I
после переноса может уйти в минус → тогда F=23 (длина цикла). Лимиты вывода у
категорий в книге разные — это presentation, движок от них не зависит.

**Дальше по этапам:** 3 — коды (Matr-зависимые: код человека/жизни, фин-код), 4 — Matr, 5 — Луна/Солнце+ЧПМ.

**Гочи:**
- ЧЖП в этой книге сводится по mod-9 (мастер-числа 11/22 НЕ сохраняются).
- Excel-файл лежит в git (добавлен заказчиком в `docs/`); локальный `.gitignore`
  игнорит `*.xlsx`, но файл уже отслеживается — это намеренный источник истины в приватном репо.

## Чек-поинт 2026-06-11 — фидбэк заказчицы (голосовое) по составу отчётов

Заказчица проверила реальные отчёты по тарифам. Разбор и статус:

| Тариф | Замечание | Корень | Статус |
|---|---|---|---|
| Прогноз 1 год | код жизни без описания + энергографик за текущий год отсутствует | `life_force_graph` рендерился только внутри секции `forecast`, а у `forecast_1y` нет атома FORECAST | ✅ FIX: график кода жизни за текущий год добавлен в блок «Коды» (report.build_calculations_section + render) — показывается во всех тарифах с `life_code` |
| Финансовый | добавить персональное число года | в `finance_1y` не было атома `personal_year` | ✅ FIX: добавлен PERSONAL_YEAR в `tariffs.finance_1y` (+тест обновлён) |
| Прогноз 5 лет | Луна-Солнце по месяцам — нужно на все 5 лет | `build_moon_sun_section` считала только текущий год | ✅ FIX: span_years=forecast_years; `monthly_years`=[{year,monthly}], рендер «по годам». Касается и matrix_full (5 лет) |
| Прогноз 5 лет | «код жизни/описание нету» | по факту «Код жизни: X» + графики по годам ЕСТЬ; описание теперь и в блоке «Коды» | ✅ покрыто FIX выше |
| Полный разбор | «график жизненных сил нету» | это тот же life_force_graph — канонический ярлык по таблице «График кода жизни» (`текст!B212`) | ✅ ЗАКРЫТО: ярлык приведён к таблице, переименовывать нечего |
| Мини-разбор | норм | — | ✅ без изменений |

**Источник истины:** отдельного текста «описание кода жизни» в Excel НЕТ — в РАСЧЕТ ячейка
«Код жизни» (число) ссылается на лист 19 «График кода жизни» (`D36=VLOOKUP(F37, текст!B213:C222)`,
`F37=INDEX(цифры_кода, age%6)` — активная цифра за возраст). Разворачиваем эту ссылку инлайном.

### PR #4 — финальное состояние (СМЕРЖЕН в main 2026-06-11 14:30 UTC, merge `80b5380`)

Три коммита, всё задеплоено (контейнер пересобран, бот polling), CI зелёный (ruff/black/gitleaks/pytest 95):
- `81d9c6f` — график кода жизни за текущий возраст в блок «Коды»; PERSONAL_YEAR в `finance_1y`;
  `build_moon_sun_section(span_years=forecast_years)` → Луна-Солнце по месяцам на все 5 лет (текст).
- `363a5e5` — **паритет PDF с текстом**: `core/pdf.py` тоже печатает график кода жизни под «Код жизни»
  и Луну-Солнце по всем 5 годам (раньше PDF отставал — правки были не видны заказчице, она получает PDF).
- `d8be0cc` — **ярлыки/описания строго по РАСЧЕТ-таблице** (правило Дмитрия «как в таблице; число =
  ссылка на описание»). Сверено по формулам книги: (1) ярлык «График кода жизни» (`текст!B212`) вместо
  выдуманного «Энергетический график на текущий год»; (2) «Жизненные силы» = число + порог-описание
  `AN15=CONCAT(Matr!C12, IF(C12>=27,"Может противостоять трудностям","Плохо переживает кризисы"))`;
  (3) «Доступ к деньгам» (legacy) = число + `AN14=CONCAT(Matr!C11, IF(C11>=27,"Открыт","Через любимое дело"))`.
  Хелперы `vitality_text`/`money_access_text` в `render.py`, переиспользованы в `pdf.py`.
  **Гочи: пороговые описания зашиты в формулу РАСЧЕТ, НЕ в листе «текст» — искать через формулы ячеек.**

**ОТКРЫТО — двуязычие RU/KK (запрос фичи):** заказчица хочет выбор языка на старте и
весь отчёт на казахском. Источник трактовок — только русский Excel, KK-контента нет.
Два пути: (1) предперевод 30 JSON-тем в KK один раз + ревью носителем (рекомендую — фикс.
тексты, контролируемое качество, без рантайм-стоимости); (2) рантайм-LLM-перевод каждого
отчёта (Groq доступен на server.zhezu.kz; риск качества на эзотерических терминах + стоимость).
Решение за Дмитрием/заказчицей; нужен KK-ревьюер.

## Чек-поинт 2026-06-11 — полный аудит листа РАСЧЕТ (PR #6–#8, смержены)

Поячеечная сверка главного листа-отчёта РАСЧЕТ (102 активные формулы) с движком.
Метод: openpyxl в двух режимах (формулы + кэш-значения), трассировка каждой
формулы до источника и до выхода бота. **Сводка — `docs/raschet-audit.md`.**

Реализовано по итогам аудита:
- **3 встроенных графика книги** (`ws._charts`) в PDF (`core/charts.py`,
  reportlab.graphics): «График жизненной энергии» (лист 19, цифры кода жизни),
  «График жизненных сил по годам» (`Matr!BE59:BE158`, `matrix.energy_curve`,
  сверено 0/100), «Энергетический график на текущий год» (`Matr!CC28:CO28`,
  `matrix.monthly_energy_year` = свод1(anchor[(m−2)%12 %5] + ((year−ГР)%9+1)),
  сверено 36/36; нумерол. год с февраля).
- **Психоматрица 15 → 17**: «Таланты» (calc!W27 = c7+c8+c9), «Духовно-материальный
  баланс» (Matr!F18 = диагональ c1+c5+c9).
- **Скрытые поля прогноза**: «Энергетический потенциал» по годам (J),
  «12-летний цикл перерождения» (AF) — выведены в текст и PDF.
- **C19 «Код тысячника»** (Matr!I3 = G3+H3, `codes.thousand_code`) и **C21 текст
  уровня сознания** (`render.consciousness_meaning`).

**Главный урок аудита:** условные/пороговые тексты РАСЧЕТ (AN14 доступ к деньгам,
AN15 жизненные силы, C19 тысячник, C21 уровень сознания) ЗАШИТЫ в формулы ячеек,
а НЕ в листе «текст» — их не найти поиском по «текст», только через формулы
(`data_only=False`). Реализованы хелперами в `core/render.py`.

**Итог: все 102 формулы РАСЧЕТ покрыты; 5 авто-тарифов собираются без ошибок;
101 тест. Аудит закрыт 100%.** Остаётся вне РАСЧЕТ: двуязычие KK (решение —
не делать сейчас); «Совместимость»/«Детские» = ручная обработка.

## Чек-поинт 2026-06-11 — двуязычие RU/KK/EN (задеплоено)

Бот стал трёхъязычным. Заказчик: «делай полный перевод с выбором языка, подключи
лингвистов» + «добавь английский».

- **Инфраструктура локалей** (обобщённая): `core/content/loader.py` ContextVar
  `_locale` + `use_locale`/`current_locale`; `_load_topic(topic, locale)` грузит
  `texts_<locale>/` с per-key fallback на ru. `core/i18n.py` `t(key, locale=None)`
  читает ContextVar — поэтому render/pdf зовут `t("ключ")` без протаскивания локали.
  `User.locale` (миграция b2c3d4e5f6a7); `report_for(..., locale)` оборачивает сборку.
- **Выбор языка**: `bot/handlers/language.py` (/lang, кнопка «🌐 Язык», ru/kk/en).
- **Перевод агентами-лингвистами** (3 воркфлоу + глоссарий): UI 161 строка, 33 темы
  контента (66 агентов), дельта 39 ключей. Ключи байт-в-байт (lookup!), значения
  переведены. Валидация всех файлов: 0 потерь ключей/типов/плейсхолдеров.
- Проверено: отчёт matrix_full полностью на kk и en (заголовки+контент+графики),
  RU без изменений. Гочи: package-data для texts_kk/en + i18n; Workflow args не
  доходит — хардкодить данные в скрипт.

PR #10 (инфра) + #11 (UI) + #12 (контент+хром) смержены. По ходу терминологию
правим в JSON/i18n без правок кода; нужен носитель-ревьюер для боевой шлифовки.

## Чек-поинт 2026-06-11 — фикс «прогноз на день пишет одно и то же» + аудит

Жалоба: 28.05.1994 + даты 11.06 и 02.06.2026 → идентичный вывод. Корень (3 фактора):
ЧПД для дней через 9 совпадает (mod-9; формула верна, golden); дневной текст
зависит только от ЧПД (1-9); большой блок «ИНДИВИДУАЛЬНЫЙ МЕСЯЦ…» одинаков весь
месяц и доминировал. Реально-дневное (биоритм-циклы) не показывалось.

Фикс (PR #14, merge 7e1fae8): daily_forecast выводит биоритм-циклы в % (физ/эмо/
интел = sin(2π·позиция/длину), циклы 23/28/33) — уникальны почти каждый день;
убран месячный блок (он есть в полном отчёте); статус биоритма → ключ + i18n.
8 биоритм-ключей переведены kk/en (208 i18n-ключей). Регресс-тест на кейс жалобы.

Аудит «аналогично остальное»: остальные выходы ОК — все скаляры отчёта меняются
по людям (5/5 уник), time-зависимые секции (ЧПГ/ЧПМ/ЧПД, прогноз, возрастной
график, благоприятные дни) корректно сдвигаются по reference_date, биоритм-статус
не залипает (traumatic/critical/favorable совпадают с днями отчёта). Бажил только
дневной прогноз.
