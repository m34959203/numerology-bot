# Roadmap (этапы разработки)

Последовательность обязательна. Каждый этап завершается прогоном тестов сверки с Excel.

## Этап 1 — Каркас + оплата (MVP-инфраструктура) ✅
Бот, меню, каталог, FSM-анкета, оплата Telegram Stars, БД, выдача.
- [x] Меню/каталог (`start.py`, `catalog.py`), карточка услуги, сид каталога.
- [x] Оплата Telegram Stars (`payment.py`): invoice XTR → pre_checkout → successful_payment,
      идемпотентность по `telegram_payment_charge_id`.
- [x] FSM-анкета (`survey.py`): ФИО + дата рождения, валидация, подтверждение.
- [x] БД-слой (`core/db.py`, `core/repositories.py`), модели, `create_all` + сид на старте.
- [x] Рендер отчёта в текст с разбивкой 4096 (`core/render.py`); выдача после анкеты.
- [x] «Мои расчёты» (`results.py`), админка (`admin.py`): статистика, возврат звёзд.
- [x] Тесты: рендер, валидаторы, идемпотентность платежей (aiosqlite). 31 passed.
Осталось для прод-запуска: реальный `BOT_TOKEN` + PostgreSQL (Alembic-миграция), приёмка.

## Этап 2 — Психоматрица + биоритмы
Перенос листов `calc` (квадрат Пифагора) и `Bio` (биоритмы) + тексты. Первая реальная выдача.
**Готово, когда:** `test_psychomatrix` и `test_biorhythm` зелёные на контрольных примерах.
- [x] Психоматрица (`calc`) — реализована, golden-тест 01.01.2000 зелёный.
- [x] Биоритмы (`Bio`) — реализованы, golden-тест зелёный (благоприятные/критические/травмоопасные дни 1:1 с книгой).
- [x] Тексты-трактовки (`текст`, 1–36) — выгружены, подключены в `report.py`, сверены.
- [x] **Полный реверс-инжиниринг всей книги** (Matr, Луна/Солнце, ЧПМ, сборка РАСЧЕТ) → `docs/excel-analysis.md`.

## Этап 3 — Коды и базовые вычисления
Полных лет, прожито дней, код жизни/человека/души, ЧЖП, финансовый код, счастливые числа (листы `РАСЧЕТ`, `Calculation`, `Lists`).
- [x] Коды от даты (`codes.py`): духовный уровень, код человека, счастливые числа,
      код жизни, фин-код, доступ к деньгам, жизненные силы, жизненная задача,
      полных лет, прожито дней — сверены на 3 эталонах (golden).
- [ ] Опасный возраст (нужна годовая таблица Солнца из Matr).
- [ ] Число имени / карма имени — после ФИО-эталонов (значения от имени).

## Этап 4 — Матрица (ядро `Matr`)
Полный реверс-инжиниринг листа `Matr` (4715 формул). Самый объёмный блок.
- [x] Прогноз на 5 лет (`matrix.py`): год, возраст, персональное число года + текст,
      Луна, Солнце, «год на +/−» + текст — сверены с эталоном (golden). В `report.py`.
- [x] Судьбоносные события (Z +/−) — цепочка aₙ₊₁=aₙ+Σцифр(aₙ); сверено на 2 примерах.
- [x] Энергетический потенциал (J) — энергокривая (опоры = первые 5 цифр кода жизни,
      линейная интерполяция /7); сверено.
- [x] Опасный возраст — первый возраст ≥ текущего с Солнцем=0; сверено (ex1=25, жен.=нет).
- [x] 12-летний цикл перерождения (AF) — digits5 кода жизни, шаг 12 лет; сверено по
      колонке BG. **Прогноз на 5 лет полностью закрыт (все колонки).**

## Этап 5 — Луна и Солнце + ЧПМ ✅
Оставшиеся методики (`луна и солнце`, `ЧПМ`).
- [x] Помесячная Луна/Солнце (`moon_sun.py`) + тексты — сверено (golden, 12 месяцев).
- [x] ЧПГ/ЧПМ/ЧПД + тексты (месяц/день/комбо) — сверено. В `report.py`.

## Этап 6 — Полировка
PDF-выдача (если нужна), админка, нагрузочная проверка, деплой, приёмочное тестирование.
- [x] PDF-выдача (`core/pdf.py`, reportlab + кириллица DejaVu); формат text/pdf/both
      (`REPORT_FORMAT`), graceful fallback на текст. Выдача через `bot/delivery.py`.
      Проверено: 6 стр., кириллица ок, теги не текут. CI ставит fonts-dejavu-core.
- [x] Админка (статистика, возврат звёзд) — в Этапе 1.
- [x] Деплой: Dockerfile (multi-stage, non-root, fonts-dejavu-core), docker-compose
      (бот+Postgres), Alembic init-миграция, entrypoint (upgrade head → polling),
      инструкция `docs/deploy.md`. Образ собран и проверен (миграции/PDF/импорты).
- [ ] Нагрузка и приёмка с заказчиком на боевом BOT_TOKEN.

## Post-launch — видео-ТЗ заказчика (2026-06-10) ✅ задеплоено

Заказчик: выдавать не «сухие цифры», а полные текстовые описания к каждому параметру
(описания уже в ячейках Excel, меняются со значением). Всё извлечено из листа «текст».
- [x] Дифференциация тарифов по полям (`tariffs.py`), 7 тарифов; ручные → к мастеру.
- [x] ЧЖП и число души — вывод по полям с подписями; персональный год полным текстом.
- [x] Кодировка «до/после 35»: 1-код = `human_code`, 2-код = `calc!E20&E21` (сверено 235/1910).
- [x] Прогноз по годам per-год: итог (что ждёт/можно/нельзя), нулевой период, график кода жизни.
- [x] Прогноз на день на выбранную дату (`daily.py`, меню / `/day`).
- [x] PR #2 смержен в main, задеплоено (Docker Compose, каталог пересижен).

## Post-launch — фидбэк заказчицы по составу отчётов (2026-06-11) ✅ PR #4 смержен

Заказчица проверила реальные отчёты по тарифам; правки строго по РАСЧЕТ-таблице.
- [x] График кода жизни за текущий возраст под «Код жизни» (все тарифы с `life_code`).
- [x] Луна-Солнце по месяцам на все годы прогноза (1/5) — `span_years=forecast_years`.
- [x] Персональное число года в финансовом тарифе (атом `PERSONAL_YEAR`).
- [x] Паритет PDF с текстом (`core/pdf.py` отставал — заказчица получает PDF).
- [x] Ярлык «График кода жизни» (`текст!B212`) вместо выдуманного; порог-описания
      «Жизненных сил»/«Доступа к деньгам» из формул `AN15`/`AN14`.
- [x] PR #4 смержен в main (merge `80b5380`), CI зелёный, контейнер пересобран.

## Post-launch — полный аудит листа РАСЧЕТ (2026-06-11) ✅ PR #6–#8 смержены

Поячеечная сверка главного листа-отчёта (102 формулы) с движком. Сводка — `docs/raschet-audit.md`.
- [x] 3 встроенных графика книги в PDF: «График жизненной энергии» (цифры кода жизни),
      «График жизненных сил по годам» (энергокривая, сверено 0/100), «Энергетический
      график на текущий год» (помесячно, сверено 36/36). `core/charts.py` + `matrix.py`.
- [x] Психоматрица 15 → 17 качеств: «Таланты» (c7+c8+c9), «Духовно-материальный
      баланс» (диагональ c1+c5+c9 = Matr!F18).
- [x] Выведены ранее скрытые поля прогноза: «Энергетический потенциал» по годам (J),
      «12-летний цикл перерождения» (AF).
- [x] Код тысячника (C19, Matr!I3) и текст уровня сознания (C21) — оба текста были
      зашиты в формулы РАСЧЕТ (не в листе «текст»), теперь реализованы.
- [x] PR #6/#7/#8 смержены, CI зелёный, контейнер пересобран. Аудит РАСЧЕТ закрыт 100%.

Сверка методик и карта извлечения — `docs/content-extraction-map.md`, аудит — `docs/raschet-audit.md`.

---

## Критерии приёмки

- [ ] Путь: меню → услуга → оплата → анкета → отчёт.
- [ ] Платёж идемпотентен; повторная выдача без новой оплаты невозможна.
- [ ] Возврат звёзд через админа.
- [ ] Каждая методика совпадает с Excel на всех контрольных примерах.
- [ ] Отчёт корректно разбивается по 4096 / формируется в PDF.
- [ ] «Мои расчёты» возвращает купленные отчёты.
- [ ] Секреты в `.env`; репозиторий чист.
- [ ] README с инструкцией запуска и деплоя.
