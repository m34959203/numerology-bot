# Открытые вопросы

> **Рамка:** заказчик дал Excel-таблицу как ЭТАЛОН своей программы. Задача — перенести
> её в Telegram-бота 1:1. Поэтому поведение расчётов берём ИЗ таблицы, а не спрашиваем.
> Сюда попадает только то, что таблица объективно не определяет.
> Статус: 🔴 ждём ответа · 🟡 есть гипотеза/обходной путь · 🟢 закрыто по таблице.

## Закрыто по таблице (бывшие вопросы)

- 🟢 **Excel-источник** получен: `docs/ПРОГРАММА МАТРИЦА на русском.xlsx`.
- 🟢 **Обязательные поля.** Для основного отчёта обязательна ТОЛЬКО дата рождения.
  Имя + имя отца нужны лишь для разделов «Финансовый прогноз» и «ЧПМ/ЧПД» (подпись BD44).
  Даты матери/отца и девичья фамилия на листе РАСЧЕТ в расчётах не используются (поля-задел).
- 🟢 **Сведение чисел.** Везде свод по mod-9 `((n-1)%9)+1`, мастер-числа 11/22/33 НЕ
  сохраняются (calc, Matr, ЧПМ — единообразно).
- 🟢 **Горизонт биоритмов.** Это presentation таблицы: воспроизводим то, что показывает
  РАСЧЕТ (в примере 7 благоприятных / 5 критических / 5 травмоопасных). Движок точен.
- 🟢 **Язык.** Контент трактовок — русский; казахский в книге только в подписях полей.
  Бот — русскоязычный (kk при необходимости позже, отдельной задачей).

## Реально нужно от заказчика

### Расчёты (нельзя проверить без данных)
- 🟢 **Число/карма имени — ЗАКРЫТО (2026-06-08).** Контрольный пример снят из книги
  напрямую (Cowork + Excel/LibreOffice): таблица транслитерации `Matr!BJ1:CP2` =
  33-буквенный алфавит, цифра = ((позиция−1) mod 9)+1; Ё/Й/Ъ/Ь — отдельные буквы.
  Реализовано в `core/numerology/name.py`, golden `tests/test_name.py` (эталон + 5 демо).
- 🟢 **Кармические события BG17/BG20 — ЗАКРЫТО (2026-06-08).** Старый `#VALUE!` был из-за
  пустых дат родителей в примере 01.01.2000. В заполненном примере (мать 06.08.1971,
  отец 23.11.1970) считается чисто. Зависят от дат родителей: BG17←мать, BG20←отец.
  Реализовано `core/numerology/karma_events.py`, golden `tests/test_karma_events.py`.
- 🟡 **Баги исходной книги** (воспроизводить как есть или чинить?): `C21` (мёртвая ветка
  духовного уровня), `AN17` карма имени (`OR(...)` всегда TRUE → канон: D7∈{13,14,16,19},
  реализован канонический флаг). По умолчанию реализуем КАНОНИЧЕСКОЕ намерение, не баг.

### Продукт / бизнес (таблица не определяет)
- 🟡 **Совместимость + детские матрицы — РУЧНАЯ ОБРАБОТКА (2026-06-10).** Заказчик решил:
  оба тарифа («Совместимость» 15 000 ₸, «Детские матрицы» 5 000 ₸) бот НЕ считает —
  карточка ведёт кнопкой на личный аккаунт мастера `https://t.me/+bZiSEb_asXZkN2M1` для
  ручной обработки (методик парного и детского разбора в Excel-источнике нет). Реализовано
  через `TariffSpec(manual=True, contact_url=...)`. Если позже появится методика —
  переключить `manual=False` и собрать состав по полям (атомы уже есть).
- 🟢 **Дифференциация тарифов по контенту — СДЕЛАНО (2026-06-10), ПО СПЕЦИФИКАЦИИ ЗАКАЗЧИКА.**
  Переход с выбора по секциям на **выбор по полям** (атомам). 5 авто-тарифов режут состав
  единого движка ровно по списку заказчика (`core/numerology/tariffs.py`): прогноз 1 год,
  прогноз 5 лет, мини, полный, финансовый прогноз. Состав каждого = `TariffSpec.fields`;
  рендер гейтит по `_fields`. См. NOTES (чек-поинт 2026-06-10).
- 🔴 **Услуги, описания, цены в звёздах.** Какие пакеты продаём, по какой цене (Stars).
- 🔴 **Порядок «оплата → анкета».** По умолчанию оплата ДО анкеты.
- 🔴 **Формат выдачи:** текст / PDF / оба (`REPORT_FORMAT`).
- 🔴 **Вывод средств:** Telegram Stars → TON/Fragment — как и когда.
- 🟡 **Возвраты.** По умолчанию — ручной возврат админом по `telegram_payment_charge_id`.
